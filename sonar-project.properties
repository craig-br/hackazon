# SonarQube server URL, e.g. http://localhost:9000
sonar.host.url=http://10.9.61.213:9000

# must be unique in a given SonarQube instance
sonar.projectKey=app-store
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=My App Store
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set. 
phpmodule.sonar.sources="classes"
sonar.exclusions=bin
# sonar.modules=phpmodule, cssmodule, jsmodule
# sonar.tests=tests
phpmodule.sonar.language=php
# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
# phpmodule.sonar.projectBaseDir="."

# Files generated by PHPUnit containing test coverage information for SonarQube
sonar.php.tests.reportPath=junit-logfile.xml
sonar.php.coverage.reportPaths=clover.xml
